※ ローカルのコマンド上で行う

# ローカルのティレクトリをリモートサーバーに転送するコマンド
scp -r /path/to/local/directory username@remote_host_ip:/path/to/remote/directory

# リモートのディレクトリをローカルPCにコピーするコマンド
sudo scp -r username@remote_host_ip:/path/to/remote/directory /path/to/local/directory
